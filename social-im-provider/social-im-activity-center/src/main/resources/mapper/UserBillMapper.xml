<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.enuos.live.mapper.UserBillMapper">

    <!--保存-->
    <insert id="save" parameterType="com.enuos.live.pojo.Bill" useGeneratedKeys="true">
        insert into user_bill (
        user_id,
        product_name,
        price,
        price_type,
        status,
        create_time
        ) values (
        #{userId},
        #{productName},
        #{price},
        #{priceType},
        #{status},
        #{createTime}
        )
    </insert>
</mapper>