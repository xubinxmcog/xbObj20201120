<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enuos.live.mapper.GroupMessageDeleteMapper">

  <!--新增数据-->
  <insert id="insertMessage" parameterType="java.util.List" useGeneratedKeys="true"
    keyProperty="id">
    INSERT INTO group_message_delete (
      group_id,
      user_id
    ) VALUES
    <foreach collection="list" item="item" index="index" separator=",">
      (#{item.groupId},
      #{item.userId})
    </foreach>
  </insert>

  <!--修改数据-->
  <update id="updateSignNum">
    UPDATE group_message_delete
       SET sign_num = sign_num + 1
     WHERE group_id = #{groupId,jdbcType=BIGINT}
       AND user_id  = #{userId,jdbcType=BIGINT}
  </update>

  <!--获得群用户信息-->
  <select id="queryMessageDateTime" resultType="java.lang.String">
    SELECT update_time
      FROM group_message_delete
     WHERE group_id = #{groupId,jdbcType=BIGINT}
       AND user_id  = #{userId,jdbcType=BIGINT}
  </select>

  <delete id="deleteGroupMessage" parameterType="java.lang.Long">
    DELETE FROM group_message_delete
     WHERE group_id = #{groupId,jdbcType=BIGINT}
  </delete>

  <delete id="deleteMessage">
    DELETE FROM group_message_delete
     WHERE group_id = #{groupId,jdbcType=BIGINT}
       AND user_id  = #{userId,jdbcType=BIGINT}
  </delete>

  <delete id="deleteGroupMessageList">
    DELETE FROM group_message_delete
     WHERE group_id = #{groupId,jdbcType=BIGINT}
       AND user_id IN
    <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
      #{item}
    </foreach>
  </delete>

</mapper>