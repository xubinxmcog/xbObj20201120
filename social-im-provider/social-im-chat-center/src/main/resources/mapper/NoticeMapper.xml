<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.enuos.live.mapper.NoticeMapper">

  <select id="getNoticeInfo" parameterType="java.lang.Long" resultType="java.util.Map">
    SELECT publish_title   AS contentTitle,
           publish_content AS contentIntro,
           create_time     AS createTime
      FROM notice
     WHERE publish_no = #{publishNo,jdbcType=BIGINT}
    </select>

</mapper>